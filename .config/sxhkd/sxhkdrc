super + Return
	$TERMINAL

super + p
	rofi -show run

super + v
	rofi -modi "clipboard:greenclip print" -show clipboard

super + {equal,minus}
	volumectl {5%+,5%-}

super + control + m
	volumectl toggle

super + BackSpace
	flameshot gui

super + control + l
	slock
